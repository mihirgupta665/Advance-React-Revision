// npm : node package manager -> is a installer which allows librarys to be installed or included in the projects
// npx : node package executer -> if you dont want to install the library and directly use it this provides that flexibillity
//npx create-react-app  project_name
// before starting we need to go to the folder of npm installed library by writing ->   [   cd lecture1   ]  and [ dir ] so that we could see our package json 
// start for to go in developer mode and build to build the  developer meaning to covert react into  html css  and javascript   { during production the build folder is only served}
// scripts are the main  to run start devlop and so on...

* create vite
// npm create vite@latest
// Project name:  vite_lecture1
│// Select a framework:  React
│// Select a variant:  JavaScript
// dev dependencies are only for devlopment and does not goes to client when built
// vite don't include node modules or packages by default so write  [  npm i ] to install the same.
// cd.. allows to go outside the latest(one) folder


