1. All the dependencies present in the package.json are available in the node module.
2. package-lock.json : this locks the depecndecies vesion for the project to be run on.
3. In public the manifest.json is used for meta-data for the mobile websites.
4. React scripts links the script file to the html file  
    vite does not have the dependency named react scripts {vite directly has a script tag in index.html  thus by not including all unnecessary stuff it makes the web page lighter}